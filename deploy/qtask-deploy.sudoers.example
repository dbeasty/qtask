# DEPRECATED — prefer user systemd (no sudo for qtask):
#   sudo /opt/qtask/live/deploy/migrate-to-user-systemd.sh
#
# Legacy optional sudoers if you must keep system-wide systemd units.
# Installed manually only when needed:
#   sudo cp qtask-deploy.sudoers.example /etc/sudoers.d/qtask-deploy && sudo chmod 440 /etc/sudoers.d/qtask-deploy
#   sudo visudo -cf /etc/sudoers.d/qtask-deploy
