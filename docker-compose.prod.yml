# Production overrides — use with:
#   docker compose -f docker-compose.yml -f docker-compose.prod.yml --profile app --profile ollama up -d

services:
  mongodb:
    ports: []

  api:
    ports:
      - "127.0.0.1:3003:3003"
    environment:
      OLLAMA_BASE_URL: http://ollama:11434
